using UnityEngine;

public class OffCamEnemyIndicator : MonoBehaviour
{
    /*
     * attach to enemy prefabs
     */
    
    public Transform player;
    /*
     * save cam dimensions
     */
    
    /*
     * draw a line from enemy to player and get intersection
     */
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        /*
         * initial check if enemy is outside of camera
         * if so then calculate closest point on edge of camera
         * make a red pointer or something to put
         */
    }

    // Update is called once per frame
    void Update()
    {
        /*
         * constant check if enemy is outside of camera
         * if so then calculate closest point on edge of camera
         * make a red pointer or something to put
         */
    }
}
